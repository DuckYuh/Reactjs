import "../styles/header.scss"
import Nav from 'react-bootstrap/Nav';

const Header = (props) => {
    return (
        <>
            <div>
                header
            </div>
        </>
    )
}

export default Header;